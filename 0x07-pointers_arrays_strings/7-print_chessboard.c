#include "main.h"

/**
* print_chessboard - prints the chessboard
* @char: array type
* @a: pointer of the array
* Return: always void.
*/
void print_chessboard(char (*a)[8])
{
	int i, j;
	for (i = 0; i < 2; i++)
	{
		for (j = 0; j < 3; j++)
	}

	_putchar(char(*a)[8])
}
