#include <stdio.h>
/**
 * main - Entry point
 *
 * Returns: Always 0 (Success)
 */
int main(void)/* description of main return value; the return value is zero hence, void */
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
